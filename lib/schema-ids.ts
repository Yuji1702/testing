/**
 * Finalized Entity IDs for Structured Data (JSON-LD).
 * These URIs serve as permanent identifiers for the website's core entities,
 * allowing AI search engines and Google to consistently map relationships
 * across different pages.
 *
 * We use fragment identifiers (#) to distinguish between the physical/professional
 * entity and the digital page it is hosted on.
 */
export const SCHEMA_IDS = {
  PHYSICIAN: "https://www.drzahidasadaf.com/#physician",
  WEBSITE: "https://www.drzahidasadaf.com/#website",
  HOMEPAGE: "https://www.drzahidasadaf.com/#homepage",
} as const;
